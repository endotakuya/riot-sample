app-main
  h2 { opts.title }
    p ほげ
  button(onclick="{ show_count }") click

  style(type="less")
    include app-main.less

  script.
    let count = 0

    show_count() {
        count += 1
        alert(`count: ${count}`)
    }
